{
  "name": "orm-typeorm",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "TypeORM 1 over SQLite from Express 5 with the decorator-free EntitySchema API: relations, the query builder with named bind parameters, and a managed transaction.",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node src/server.js",
    "seed": "node src/seed.js"
  },
  "dependencies": {
    "better-sqlite3": "^13.0.3",
    "express": "^5.2.1",
    "typeorm": "^1.1.1",
    "zod": "^4.5.4"
  },
  "overrides": {
    "typeorm": {
      "better-sqlite3": "^13.0.3"
    }
  }
}
