{
  "name": "file-upload",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Multer 2 uploads with every limit applied, magic-byte content sniffing, UUID filenames and storage outside any static root.",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^5.2.1",
    "helmet": "^8.3.0",
    "multer": "^2.3.0"
  },
  "devDependencies": {
    "supertest": "^7.2.2"
  }
}
