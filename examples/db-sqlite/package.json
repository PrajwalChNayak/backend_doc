{
  "name": "db-sqlite",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "SQLite from Express 5 two ways: the built-in node:sqlite DatabaseSync and better-sqlite3, side by side behind one API.",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "better-sqlite3": "^13.0.3",
    "express": "^5.2.1",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "supertest": "^7.2.2"
  }
}
