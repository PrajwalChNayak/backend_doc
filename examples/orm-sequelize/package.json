{
  "name": "orm-sequelize",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Sequelize 6 over SQLite from Express 5: model definitions, associations with eager loading, a managed transaction, and replacements vs bind in sequelize.query().",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node src/server.js",
    "seed": "node src/seed.js"
  },
  "dependencies": {
    "express": "^5.2.1",
    "sequelize": "^6.37.8",
    "sqlite3": "^6.0.1",
    "zod": "^4.5.4"
  }
}
