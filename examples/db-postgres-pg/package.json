{
  "name": "db-postgres-pg",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "PostgreSQL from Express 5 with node-postgres: pool, $1 placeholders, a withTransaction helper, upserts and timeouts.",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node src/server.js",
    "migrate": "node src/migrate.js"
  },
  "dependencies": {
    "express": "^5.2.1",
    "pg": "^8.23.0",
    "zod": "^4.5.4"
  }
}
