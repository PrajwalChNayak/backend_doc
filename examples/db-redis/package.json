{
  "name": "db-redis",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Redis from Express 5 with node-redis: cache-aside with a TTL, SET NX EX locks, SCAN instead of KEYS, and pub/sub on its own connection.",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node src/server.js"
  },
  "dependencies": {
    "express": "^5.2.1",
    "redis": "^6.2.1",
    "zod": "^4.5.4"
  }
}
