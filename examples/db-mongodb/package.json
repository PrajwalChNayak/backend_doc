{
  "name": "db-mongodb",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "MongoDB from Express 5 with the official driver: one reused MongoClient, indexes at boot, CRUD, and an operator-injection-proof login route.",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node src/server.js"
  },
  "dependencies": {
    "express": "^5.2.1",
    "mongodb": "^7.6.0",
    "zod": "^4.5.4"
  }
}
