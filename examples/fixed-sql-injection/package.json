{
  "name": "fixed-sql-injection",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "The vulnerable-sql-injection API, fixed: parameterised statements throughout, plus an allow-listed ORDER BY.",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "better-sqlite3": "^13.0.3",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "supertest": "^7.2.2"
  }
}
